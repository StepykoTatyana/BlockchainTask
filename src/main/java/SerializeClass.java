import java.io.IOException;

public class SerializeClass {
    public static long getDatePrevious(BlockChain blockChain) {
        String fileName = "blockchains.data";
//        try {
//            SerializationUtils.serialize(blockChain, fileName);
//            BlockChain[] blockChainList = (BlockChain[]) SerializationUtils.deserialize(fileName);
//            return blockChainList[blockChain.getId() - 2].getTimestamp();
//        } catch (IOException | ClassNotFoundException e) {
//            e.printStackTrace();
//            return 0;
//        }
        return 0;
    }
}
